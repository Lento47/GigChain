# ============================================
# GigChain.io Backend Dependencies - Pinned
# ============================================
# Generated: 2025-10-06
# Python: >=3.10,<3.13

# Core Testing
pytest==8.3.3
pytest-cov==5.0.0
pytest-asyncio==0.24.0

# OpenAI Integration
openai==1.54.3

# FastAPI and ASGI Server (Primary Framework)
fastapi==0.115.4
uvicorn[standard]==0.32.0
gunicorn==23.0.0

# Production Dependencies
python-dotenv==1.0.1
pydantic==2.9.2
pydantic-core==2.23.4
pydantic-settings==2.5.2  # BaseSettings moved to separate package in v2
python-multipart==0.0.17

# HTTP Clients
requests==2.32.3
httpx==0.27.2
certifi==2024.8.30

# Web3 & Wallet Authentication (W-CSAP)
web3==7.4.0
eth-account==0.13.4
eth-utils==5.1.0
eth-typing==5.0.1
eth-hash[pycryptodome]==0.7.0
hexbytes==1.2.1
mnemonic==0.21  # BIP39 mnemonic for wallet recovery

# Cryptography & JWT
cryptography==43.0.3
pycryptodome==3.21.0
PyJWT[crypto]==2.9.0  # JWT tokens with cryptographic signing
ecdsa==0.19.0  # ECDSA signature verification for DPoP (ES256K)

# MFA & TOTP (Multi-Factor Authentication)
pyotp==2.9.0  # Time-based One-Time Passwords
qrcode==7.4.2  # QR code generation for TOTP
pillow==10.4.0  # Image processing for QR codes

# Database - Production Scaling (PostgreSQL)
psycopg2-binary==2.9.9  # PostgreSQL adapter for Python
sqlalchemy==2.0.23  # Optional: ORM for advanced queries (if needed)

# Caching & KMS (Optional - for advanced features)
redis==5.0.8  # Token revocation cache
# boto3==1.35.0  # AWS KMS (uncomment if using AWS)
# hvac==2.3.0  # HashiCorp Vault (uncomment if using Vault)

# IPFS Integration
ipfshttpclient==0.8.0a2  # IPFS Python client (alpha - wrapped in adapter for stability)
py-cid==0.3.0  # Content Identifier utilities
tenacity==9.0.0  # Retry library with exponential backoff

# Additional utilities
typing-extensions==4.12.2
annotated-types==0.7.0
idna==3.10
charset-normalizer==3.4.0
urllib3==2.2.3

# Development and Code Quality (optional, for dev environment)
# Uncomment these for development:
# ruff==0.7.3
# black==24.10.0
# mypy==1.13.0
# pre-commit==4.0.1

# Security scanning (optional, for CI/CD)
# safety==3.2.8
# bandit==1.7.10

# Documentation (optional)
# mkdocs==1.6.1
# mkdocs-material==9.5.44

# Note: Use pip-tools or uv for lock file generation:
# pip install pip-tools
# pip-compile requirements.txt --output-file requirements.lock
